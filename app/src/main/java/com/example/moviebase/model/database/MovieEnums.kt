package com.example.moviebase.model.database

class MovieEnums {

    enum class MovieType {
        POPULAR,
        NOW_PLAYING,
        TOP_RATED,
        UPCOMING
    }

    enum class ExtraType {
        RECOMMENDED,
        SIMILAR
    }
}
